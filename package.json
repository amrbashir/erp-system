{
  "name": "erp-system",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "backend": "pnpm run --filter @erp-system/backend --",
    "web": "pnpm run --filter @erp-system/web --",
    "sdk": "pnpm run --filter @erp-system/sdk --",
    "utils": "pnpm run --filter @erp-system/utils --",
    "build": "pnpm utils build && pnpm backend build && pnpm sdk build && pnpm web build",
    "test": "pnpm build && pnpm -r test",
    "lint": "pnpm build && pnpm -r lint",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  },
  "packageManager": "pnpm@10.12.1"
}
