model InvoiceItem {
  id String @id @default(uuid())

  description    String
  purchase_price Int
  selling_price  Int
  quantity       Int

  invoice   Invoice @relation(fields: [invoiceId], references: [id])
  invoiceId String

  @@map("invoice_items")
}
