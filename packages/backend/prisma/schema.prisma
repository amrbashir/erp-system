generator client {
  provider            = "prisma-client"
  output              = "../src/prisma/generated"
  importFileExtension = "js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
